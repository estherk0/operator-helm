apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eck.fullname" . }}-test-connection"
  labels:
{{ include "eck.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "eck.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
